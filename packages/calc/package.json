{
  "name": "@mondash/calc",
  "version": "0.0.0",
  "packageManager": "yarn@3.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "check:lint": "yarn g:lint $INIT_CWD",
    "check:style": "yarn g:style --check $INIT_CWD",
    "check:types": "yarn build"
  },
  "files": [
    "dist",
    "!__tests__",
    "!*.test.*"
  ],
  "devDependencies": {
    "@types/node": "^16.4.12"
  },
  "dependencies": {
    "@mondash/math": "workspace:packages/math"
  }
}
